import { ObjectUnifierPipe } from './object-unifier.pipe';

describe('ObjectUnifierPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectUnifierPipe();
    expect(pipe).toBeTruthy();
  });
});
