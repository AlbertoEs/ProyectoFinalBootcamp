import { ChronologyPipe } from './chronology.pipe';

describe('ChronologyPipe', () => {
  it('create an instance', () => {
    const pipe = new ChronologyPipe();
    expect(pipe).toBeTruthy();
  });
});
